services:
  pr-context:
    build: .
    env_file: .env
    environment:
      - TRANSPORT=sse
      - PORT=8321
    ports:
      - "8321:8321"
    volumes:
      - pr-data:/data
    restart: unless-stopped

volumes:
  pr-data:
